# Reproducing every number in DOCUMENT.md

Dependency-ordered map from each claim to the script that produced it, following
the convention of `awd_clean/manuscript/REPRODUCE.md`. `FIGURE_NUMBERS.txt` is
generated by `make_figures.py` and lists every plotted value with its source
product; it is the machine-checked half of this file.

All paths are relative to `notebooks/awd_clean/`. Environment: `ml gcc/12.4.0`,
`conda activate das`, or the absolute interpreter
`/home/users/nberrios/miniconda3/envs/das/bin/python`. All jobs go to `-p serc`.

## Build order

```bash
cd notebooks/awd_clean/fig7c_negative
python make_figures.py      # or: sbatch figs_job.sh
python3 assemble.py         # sections/*.md -> DOCUMENT.md; never edit DOCUMENT.md
```

`assemble.py` fails if a figure is referenced without a caption, if a caption is
never referenced, or if a referenced PNG is missing. `make_figures.py` fails if
any input product is absent rather than drawing a partial figure.

`assemble.py` concatenates `sections/*.md` in `sorted()` order and does **not**
check section numbers, so a new file has to be named to land in the right place.
The current order is `00_abstract`, `01_introduction`, `02_data_methods`,
`03_results` (4.1-4.5), `03b_afk` (4.6, generated by `append_afk_results.py`),
`03c_convergence` (4.7), `03d_illumination` (4.8-4.9), `04_discussion`,
`05_limitations`. Note that `03_results` sorts before `03b` because `_` < `b`.

## Section 4.1 -- implementation validation

| claim | value | script | product |
|---|---|---|---|
| picks from Lellouch's released 7d traces | 2,416 m/s at 50 m to 4,357 m/s at 700 m | `ambient_lellouch_fig7d_profile.py` | `.txt` lines 29-43 |
| depth-velocity correlation on those picks | r = 0.948 | same | `.txt` "fit r" |
| same picker on our data | 114 of 171 fibre positions finite, median 26,382 m/s | same | `.npz` `v_median` |

**Read this before quoting r = 0.948.** It is `np.corrcoef(lel_z, lel_v)` at
`ambient_lellouch_fig7d_profile.py:213` -- the correlation between depth and the
velocities our picker returns from *his* traces. It is **not** agreement with his
published Figure 9 curve, which is not digitised in this repository. The `.npz`
key `r` is a *different* quantity: the velocity-depth trend of **our** data,
r = -0.445. Confusing the two produced an overstatement in an earlier draft and in
this project's planning document.

## Section 4.2 -- the non-reproduction

| claim | value | source |
|---|---|---|
| single day 2024-12-20, familywise p | 0.1470 (10,000 nulls) | `ambient_transfer/lellouch2019_exact_stack/_authoritative_38993456/aggregate_2024-12-20_src23_ram0p1_cross_correlation_ordered_r0.npz` |
| minimum p over six days | 0.1345 (2024-11-30, 21.3 h) | `FIG7C_MULTIDAY_RESULT.md` day table |
| Fisher combination, five independent days | chi-sq 9.08 / 10 df, p = 0.524 | `FIG7C_MULTIDAY_RESULT.md` |
| four-day coherent stack | p = 0.9184, 23,036 windows | `ambient_lellouch2019_multiday_stack.npz` (`p_value`, `n_windows`, `dates`) |
| detectability of that stack | 0.72 | `FIG7C_MULTIDAY_RESULT.md` |
| per-velocity null | 0 of 181; 2.752 vs 2.811 at 3,200 m/s | `FIG7C_MULTIDAY_RESULT.md` |
| pedestal diagnostic, baseline / common-mode | +0.976 / -0.381 | the two aggregates above |
| pairs reaching p < 0.05 | 2 of 10; p = 0.0390 and p = 0.0165 | `_fig7c_pair_survey.py`, `fig7c_pair_survey.txt` |

**Do not quote either pair as a positive result, and do not use multiplicity as
the reason.** 2 hits in 10 pairs against 0.5 expected is *above* chance, so the
multiplicity argument is weak; the objection that carries is velocity. Both pairs
peak at 5,850 m/s -- the scan ceiling -- with causal/acausal ratios at 3,200 m/s of
0.97 and 1.01, which is the pedestal signature rather than an arrival. An earlier
version of this file and of section 4.2 reported only the p = 0.039 pair and called
it the extreme of the distribution; it is neither the only hit nor the extreme.

Per-day peak velocities are 5,850-5,925 m/s on the five 500 Hz days and 1,675 m/s
on 2024-05-11, which was recorded at 5,000 Hz and is excluded from the stack.
Causal/acausal at 3,200 m/s is below 1 on 3 of the 6 days and never above 1.13.

## Section 4.3 -- constant-offset isolation

`ambient_lellouch2019_fig7d.py` -> `ambient_lellouch2019_fig7d.npz`
(`ours_plain`, `pick_plain`, `lellouch_z`, `lellouch_v`, `depths`, `lags`).
His lags 20.7 -> 11.5 ms (= 50 m / `lellouch_v`, i.e. 50/2415.89 and 50/4356.96).

Our side is 0.0000 s at **13 of the 14** depths, not all 14. The 50 m pair has its
global maximum at +0.114 s and `pick_plain[0]` = 1,010.23 m/s (an implied 49.5 ms,
outside the picker's own 5-45 ms window because the parabolic correction is
unbounded); the other 13 are NaN. `FIGURE_NUMBERS.txt` `fig3_finite_picks` records
this as "1 of 14", and section 4.3 now states it the same way.

Note that this product is the **common-mode-removed** branch (its `.txt` header says
`common-mode removal: True`), so Figure 3(a) is the post-removal gather. No
pre/post amplitude ratio can be formed from it; an earlier version of section 4.3
quoted "a factor of about 120", which no product on disk supports, and it has been
removed.

## Section 4.4 -- the static fixed-k pattern

```bash
CENSUS_FMIN=5  CENSUS_FMAX=12 K0_REMOVE=1 python ambient_apparent_velocity_census.py
CENSUS_FMIN=12 CENSUS_FMAX=20 K0_REMOVE=1 python ambient_apparent_velocity_census.py
python ambient_fixed_k_test.py      # reads both, computes the centroid comparison
```

| claim | value | source |
|---|---|---|
| band centres | 8.50 Hz, 16.00 Hz; ratio 1.882 | `ambient_fixed_k_test.txt` |
| \|k\| centroids | 0.00173, 0.00175 cyc/m; ratio 1.011 | same |
| low-k share of in-band energy | 39.33 %, 39.08 % | same |
| a 3,200 m/s arrival would sit at | 1.86 and 3.50 cells | same |

`ambient_fixed_k_test.py` also prints a **secondary** peak-per-frequency
diagnostic that returns no verdict. That is expected and does not retract the
centroid result: excluding +-2 cells around k = 0 pushes its peak to ~0.019 cyc/m,
the genuine surface-wave energy, and its own permutation control shows "static"
winning on 1578/2000 frequency-shuffled sets.

## Section 4.5 -- the eight methods

| method | script |
|---|---|
| fixed-fan f-k, brick wall | `ambient_fk_transfer_test.py`, `ambient_signed_fk_v2.py` |
| fixed-fan f-k, raised cosine | `ambient_fk_taper_test.py` |
| tau-p slant stack | `ambient_radon_slant_stack.py` |
| rank-k subspace | `--svd-rank` in `ambient_lellouch2019_exact_stack.py` |
| PCC and PWS | `ambient_pcc_pws.py` |
| offset-axis median | `ambient_flat_event_removal.py` |
| median / mean common mode | `--common-mode`, `--common-mode-estimator` |

Figure 4's bars are read from those products by `make_figures.py:fig4`, which finds
seven: the untreated baseline plus median common-mode, flat-event median at two
settings, PCC, PCC+PWS and tau-p. The two fixed-fan f-k families and rank-k have no
saved pedestal diagnostic, which is why seven bars accompany a table of eight
methods.

Aperture arithmetic (1.09 cells at 5 Hz, 4.38 at 20 Hz) is printed by
`ambient_apparent_velocity_census.py`'s header. The script's own comment says 4.37;
the `.txt` products say 4.38; the exact value is 4.375.

Three numbers in this section are **not** carried by any saved product and are
labelled as such in the text:

| number | where it lives | status |
|---|---|---|
| mean common-mode `corr = +0.888` | comment at `ambient_apparent_velocity_census.py:136` | not recomputable from disk |
| rank-k pedestal +0.96 to +0.98 | comment at `ambient_lellouch2019_exact_stack.py:284` | not recomputable from disk |
| taper "corrected ratio 1.51-2.01" | nowhere | withdrawn; do not requote |

The tau-p mute is **6,000 m/s**, from `ambient_radon_slant_stack.txt`
(`p mute: |p| < 1.667e-04 s/m`). 8,000 m/s is only the script's `--p-mute` default,
and the run overrode it; there is no mute sweep anywhere in the script or product.
The pedestal *worsens* across the mute, +0.740 to +0.940.

## Section 4.6 -- adaptive f-k

```bash
sbatch afk_recovery.sbatch          # array 0-4, prediction fixed in the header
python fig7c_negative/append_afk_results.py
```

Implementation `adaptive_fk()` in `ambient_lellouch2019_exact_stack.py`; unit
checks in `_afk_unit_check.py`, whose output is now saved as `_afk_unit_check.txt`
(alpha = 0 bit-identical; overlap-add rel err 0.000e+00; plane-wave coherence
0.0523 -> 0.1980 at the production alpha = 1, 0.1231-0.1968 at the other five
exponent/normalisation settings; no moveout manufactured from pure noise).
Configurations and the pre-run prediction are in the sbatch header.

```bash
python _afk_unit_check.py > _afk_unit_check.txt      # regenerate the unit-check log
```

## Section 4.7 -- stack convergence

```bash
sbatch convergence_job.sh           # or: python ambient_stack_convergence.py
```

Reads the resumable hourly chunks under
`ambient_transfer/lellouch2019_exact_stack/` for 2024-12-20 and re-aggregates them
at 1, 2, 4, 8, 16 and 24 chunks in both branches, rebuilding the receiver-order
null at every stack length.

| claim | value | source |
|---|---|---|
| detectability exponent, baseline | N^+0.042 | `ambient_stack_convergence.txt`, refitted in `make_figures.py:fig8` |
| detectability exponent, common-mode removed | N^+0.019 | same |
| raw-score exponents | N^+0.158 / N^-0.004 | same |
| the threshold crossing | detect 1.014, p 0.0170 at 16 chunks | same |
| and its reversal | detect 0.971, p 0.1614 at 24 chunks | same |
| pedestal at the full common-mode-removed stack | -0.219 | same |

**Read detectability, not the raw score.** The raw score can rise because a
repeatable contaminant accumulates. Detectability divides by the 95th percentile of
the null rebuilt at the same stack length, so a contaminant raises both terms.
`make_figures.py:fig8` refits both exponents from the `.npz` arrays with the same
log-log `polyfit` the producing script uses (`ambient_stack_convergence.py:182`), so
the figure and the `.txt` cannot drift apart.

## Section 4.8 -- illumination

```bash
python interrogator_and_illumination_v2.py     # rank sweep, both epochs
sbatch illum_scan_job.sh                       # 240-window archive scan
```

| claim | value | source |
|---|---|---|
| 2017 \|A\|, ranks 0/1/2 | 0.348 / 0.329 / 0.250, p = 0.0050 / 0.0050 / 0.0299 | `interrogator_and_illumination_v2.txt` |
| 2024-25 \|A\|, ranks 0/1/2 | 0.040 / 0.040 / 0.036, p = 0.7307 / 0.4314 / 0.4638 | same |
| archive scan | 240 of 240 windows; 11 hits; 12.0 expected; ceiling 18 | `illumination_window_scan.txt`, `.npz` |
| \|A\| median / max | 0.0296 / 0.2385 | same |

The decision rule (hits must exceed the 95th percentile of Binomial(N, 0.05)) is
in the script docstring, which was written before the run.

`lellouch2017_window_audit.py` establishes that the 2017 comparison window is
pre-event noise: arrivals at 4.100 s and 4.916 s of 5.00 s, both after the 2.5 s
cut, with pre-arrival k = 0 shares of 0.13 % and 0.11 %.

## Section 4.9 -- interrogator

```bash
python interrogator_blame_test.py          # channels 0-699, includes lead-in
python interrogator_and_illumination_v2.py # channels 23-708, as analysed
```

| range | cross-day median \|corr(u1,u1)\| | control 95th |
|---|---|---|
| 0-699 | 0.8426 | 0.3889 |
| 23-708 | 0.0188 | 0.1282 |

The two rows come from two different runs and their controls are not
interchangeable: the 0-699 control is n = 120 (four sub-leading patterns x 6 days x
5 others), the 23-708 control is n = 150 (five sub-leading patterns).

Lead-in to deep power ratio in u1: `interrogator_blame_test.txt` T2 gives per-day
values 323.37, 26530.29, 128079.92, 20873.86, **0.01**, 24799.78, median 22836.82.
The "2e4 to 1.3e5" quoted in `interrogator_and_illumination_v2.txt` describes four of
the six days only; section 4.9 now states the range with that qualification. The
same product warns that the lead-in records real surface ground motion, so a high
ratio is not on its own proof of an instrumental origin.

Gauge-length attenuation 0.1-1.1 % at 3,200 m/s.

## Data

`/oak/stanford/groups/ettore88/data/SAFOD/SAFODAS1-harddrive-transfer/`, manifest
`SAFOD_2024_2025.csv`, 377,680 continuous 60 s records, 2024-05-21 to 2025-05-06.
Lellouch's release staged at `/scratch/users/nberrios/lellouch2017/` (`M1p33`,
`M2p46`, `README.md`); correlograms in `awd_clean/lellouch_traces/`.

**Never read 2024-25 data through `DASutils.readFile_HDF` for any k = 0 or
common-mode measurement.** Its `median=True` default subtracts the per-sample
median across channels and removes the quantity under test. Every script here
uses `h5py` directly. This is the confound that invalidated
`faultzone/repeaters/cross_epoch_noise_floor.py` and it also rules out the 206
cached events in `faultzone/repeaters/cache_all/` for such measurements.

## Numbers in the document that a product does not carry

Every one of these is flagged in the text where it appears. Listed together so an
auditor does not have to rediscover them.

| number | section | situation |
|---|---|---|
| mean common-mode `corr = +0.888` | 4.5 | source comment only |
| rank-k pedestal +0.96 to +0.98 | 4.5 | source comment only |
| taper "corrected ratio 1.51-2.01" | 6.3 | nowhere on disk; withdrawn claim, do not requote |
| "81-99 % of energy below 1,500 m/s" | 6.3 | verbatim quote of a withdrawn claim; the products span 80.9-98.1 % |
| "26 m in 2017" | 6.3 | verbatim quote of a withdrawn claim; the re-run product gives 400 m |
| p = 0.1345, detectability 0.72, chi-sq 9.08, the 3.5 % moveout-free control | 4.2, 7 | carried by `FIG7C_MULTIDAY_RESULT.md`, which is a status document rather than a script product |
| Lellouch's "especially helpful in seismically quiet areas" | 1 | direct quotation from the paper PDF; no PDF text extractor is installed on this cluster, so it cannot be checked here |

## Status documents that govern interpretation

- `AMBIENT_LOWK_MECHANISM.md` -- the static pattern; its header states that
  illumination (section 4.8) is upstream of it
- `AMBIENT_CC_LITERATURE_REVIEW.md` -- the review, and section 4a on what
  controls illumination, with the withdrawn field-deployment speculation marked
- `Ambient_FK_QC_workflow.ipynb` -- the fixed-fan F-K verdict, which still stands
- `FIG7C_MULTIDAY_RESULT.md` -- the multi-day tables
- `AMBIENT_FIG7C_STATUS.md` -- Natalia's status document; section 4 is hers and
  is not edited without her sign-off
